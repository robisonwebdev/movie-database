import React from 'react';
import '../../../styles/main/movie/MovieCard.css';

const MovieCard = () => {
    return (
        <section className='movie_card'>MovieCard</section>
    );
};

export default MovieCard;