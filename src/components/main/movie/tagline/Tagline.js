import React from 'react';
import '../../../../styles/main/movie/tagline/Tagline.css';

const Tagline = ({ movie }) => {
    const { tagline } = movie;

    return (
        <h4>Tagline</h4>
    );
};

export default Tagline; 