import React from 'react';
import '../../../styles/main/person/PersonKnownFor.css';

const PersonKnownFor = ({ credits, format }) => {
    return (
        <section>PersonKnownFor</section>
    );
};

export default PersonKnownFor;